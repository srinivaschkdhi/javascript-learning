const geocode = (address,callback)=>{
    setTimeout(() => {
        const dataa = {
            latitude:0,
            longitude:0
        }
        return dataa;
    }, 2000);
}

const data = geocode('Philadelphia')
console.log(data)